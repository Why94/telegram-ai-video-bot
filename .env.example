# Environment Variables Template
# Copy this file to .env and fill in your values

# Required: Telegram Bot Token from @BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Provider API Keys (Set at least one of these to use the corresponding provider)
BYTEPLUS_API_KEY=your_byteplus_api_key_here
KLING_API_KEY=your_kling_api_key_here
MINIMAX_API_KEY=your_minimax_hailuo_api_key_here
LUMA_API_KEY=your_luma_api_key_here
RUNWAY_API_KEY=your_runway_api_key_here
GOOGLE_VEO_KEY=your_google_cloud_credentials_here
LEONARDO_API_KEY=your_leonardo_api_key_here
REPLICATE_API_KEY=your_replicate_api_key_here
RUNNINGHUB_API_KEY=your_runninghub_api_key_here
FREETHEAI_API_KEY=your_freetheai_api_key_here

# KIE.ai API Key (80 free credits for new users, no credit card needed)
# Sign up at https://kie.ai → get key at https://kie.ai/api-key
KIE_API_KEY=your_kie_api_key_here

# Optional: Default active provider (byteplus, kling, hailuo, luma, runway, veo, replicate, runninghub, freetheai, kie)
DEFAULT_PROVIDER=byteplus

# Optional: Default video settings
DEFAULT_RESOLUTION=720p
DEFAULT_ASPECT_RATIO=16:9
DEFAULT_DURATION=auto
GENERATE_AUDIO=true

# ─── Admin Panel (Bulk Account Inventory) ────────────────────────────────────
# Comma-separated Telegram user IDs allowed to manage inventory (SUPER_ADMIN/ADMIN)
ADMIN_USER_IDS=123456789
# Secret key for encrypting account passwords at rest (use a long random string)
INVENTORY_ENCRYPTION_KEY=change_me_to_a_long_random_secret_key
# Path to the SQLite database file (optional, defaults to ./data/inventory.db)
INVENTORY_DB_PATH=./data/inventory.db
# Low stock warning threshold per product (optional, default 10)
INVENTORY_LOW_STOCK=10

# ─── Payment Gateway (Xendit) ─────────────────────────────────────────────────
# Isi dengan API key dari dashboard.xendit.co (atau sandbox). Kosongkan untuk nonaktifkan /pay.
XENDIT_API_KEY=
# Webhook verification token (Settings → Webhooks → Verification Token)
XENDIT_WEBHOOK_TOKEN=
# 1 credit = berapa Rupiah (default 1000 → 100 credit = Rp100.000)
CREDIT_RATE_IDR=1000
# PUBLIC_URL / Railway domain untuk callback (opsional, Xendit pakai default-nya)
# PUBLIC_URL=https://your-app.up.railway.app
PORT=3000

# ─── Manual Top Up (used while Xendit business verification is pending) ─────────
# Rate: 1 credit = berapa Rupiah
CREDIT_RATE_IDR=1000
# Nomor tujuan transfer (tampil ke client di menu Top Up)
MANUAL_DANA=08xxxxxxxxx
MANUAL_OVO=08xxxxxxxxx
MANUAL_BCA=xxxxxxxxxx
# Username/admin untuk dihubungi client
MANUAL_PAYMENT_ADMIN=@admin_username
# Atau set JSON lengkap (opsional, menimpa di atas):
# MANUAL_PAYMENT_INFO={"ratePerCredit":1000,"methods":[{"name":"DANA","number":"08xxx"}],"adminContact":"@admin","note":"..."}

# ─── Leonardo AI (image-to-video) ──────────────────────────────────────────────
# Biaya tetap per generate (credit). Default 2000.
LEONARDO_CREDIT_COST=2000
# Hanya produk akun ini yg tampil di Beli Akun (pisah koma). Default: Leonardo AI
ALLOWED_ACCOUNT_PRODUCTS=Leonardo AI
